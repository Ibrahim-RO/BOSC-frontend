// TYPE PARA PREGUNTAS
export type DataQuestionsType = {
    title: string
    content: string
}

// TYPE DE CARDS
export type DataCardType = {
    label: string;
    img: string;
}

// TYPE FORM
export type FormType = {
  fullName: string;
  email: string;
  state: string;
  service: string;
  message: string;
}
